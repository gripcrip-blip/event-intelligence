[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"

# Do not set publish for Next.js — OpenNext adapter manages it.
# Env vars (Site settings → Environment variables):
#   NETLIFY_DB_URL / DATABASE_URL — via @netlify/database on first deploy
#   PANDASCORE_API_TOKEN
#   PANDASCORE_API_BASE_URL = https://api.pandascore.co
